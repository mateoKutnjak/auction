truffle migrate                 # creating new smart contract
truffle migrate --reset         # changing old smart contract
                                # reruns all migrations from the beginning
truffle console
>> app.candidates(1)
>> app.candidates(1).then(function(c) {candidate = c;})
>> candidate.voteCount.toNumber()
>> web3.eth.getAccounts()
>> web3.eth.getAccounts().then(function(c) {a=c;}); a[0]

nom run dev                     # start lite server

>> app.vote(1, {from: a[0]})    # vote


### MetaMash secret phrases: account comic ball skill gadget slice pull mom enact scare grain fresh